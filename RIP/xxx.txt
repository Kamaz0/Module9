W pliku app.py importujemy także formularz TodoForm. Przygotuj go samodzielnie, korzystając z takich typów pól jak StringField, TextAreaField i BooleanField. Typy pól i ich nazwy odgadniesz patrząc na kod szablonu.










routing, szablon, diedzicznecznei szablon (formularze?)


















    # id = db.Column(db.Integer, primary_key=True)
    # text = db.Column(db.String(200)) 
    # complete = db.Column(db.Boolean) 
        # def __repr__(self): 
        # return self.text 